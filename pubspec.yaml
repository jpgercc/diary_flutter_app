name: fashion_store_app
description: An elegant fashion store app in Flutter.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  carousel_slider: ^5.0.0 # Para o carrossel de imagens na tela de detalhes
  provider: ^6.0.5 # Para gerenciamento de estado simples do carrinho
  google_fonts: ^5.1.0 # Para fontes personalizadas (opcional, mas adiciona elegância)


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/